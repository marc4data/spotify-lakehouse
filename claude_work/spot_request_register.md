# Request Register — spot

Cowork owns this file. Sessions never mint their own numbers.
Format: `spot-<session>-R-###`. Bare numbers below; the session is the section heading.

Status: ✅ landed · 🔨 in flight · 📋 queued · ⏸️ parked · 💭 deferred · ❓ unverified · ⚠️ dropped

---

## spot-main

| ID | Status | Request | Rounds | Notes |
|---|---|---|---|---|
| R-001 | ✅ | **Project CLAUDE.md and governing documentation** | 0 | "Build a claude.md for this project folder to establish the guidelines for this effort." Delivered 2026-09-13: `CLAUDE.md`, `docs/data-contracts.md`, `docs/worktree-protocol.md`, `docs/notebook-specs.md`. |
| R-002 | ❓ | **Repo scaffold, uv env, Docker Postgres, dbt project, OAuth, first verified API call** | 1 | Built at `3a499cf`, report at `f28d3a5`, `account_id` redaction at `4b5962b`. Cowork verified against the repo 2026-09-13: tracked-file list clean, full-history secret scan clean across all 3 commits, `.gitignore` correct, five guards documented with named red tests. **Runtime claims (ids 5–8, live probes from `main` and `wta`) are unverifiable from Cowork — no reachable Docker or psql.** Stays ❓ until R-018 re-exercises the probe and produces fresh rows, which settles it without asking Marc again. Everything Cowork *can* check from the repo is verified. |
| R-003 | ⏸️ | **Ingest Extended Streaming History export files** | 0 | Parked on Spotify delivery (~weeks). Loader is built against the documented schema and a synthetic fixture; real files swap in on arrival. |
| R-004 | ✅ | **Kimball star schema in dbt-postgres** | 1 | Contract in `docs/data-contracts.md` §2–§4, amended from the R-002 probe (F1–F5, F7–F9). **Scoped to the API path** — export loader deferred to R-003, genre bucket seed shipped empty pending R-015, playlists deferred to R-006. Runs in `main`: it touches migrations and dbt schemas, and `main` is the only session that may later build into `analytics`. **Round 1 stopped correctly on a contract conflict** (`c8f0a65`) — Spotify returns no artist `genres` at all, 55/55 objects across three endpoints. Cowork had written "still returns but deprecated" from the API reference and presented it as live behaviour. Contracts §1, §3, §4 amended and the prompt rebuilt with every decision the report asked for. **LANDED 2026-09-14 at `cff07c0`**, verified by Cowork against the repo: 20 model files on disk (4 staging / **7** intermediate / 9 marts), migrations `0002` and `0003` present, `scripts/dbt.sh` no longer sources the credentials file and exports no `SPOTIFY_*` to dbt, and the only credential-shaped tracked files are `.env.example` and `profiles.csv.example`. Round reports PASS=114 dbt nodes, 92 pytest, and both required breaks red on named tests. Prompt: `prompts/spot-main-R-004.md`. |
| R-005 | 📋 | **Multi-profile utility — swap between Marc, Marie, Brody, Emma** | 0 | "the account profile utility should be able to swap between the profiles." `dim_profile.profile_slug` is the address. Export-only contributors get a profile too. |
| R-006 | 📋 | **Playlist inventory, overlap and miss analysis** | 0 | "explore/inventory my playlists, compare playlists to overlaps and misses." Contracts §5 — miss is directional. **Now three match tiers, not two:** URI, ISRC, normalized name. ISRC came out of the R-002 probe and is the real fix for single-vs-album. |
| R-007 | 📋 | **Tableau Public extract publication** | 0 | Tableau Public cannot source a database. `published/` emits flat extracts from marts; `make publish` regenerates. |
| R-008 | 📋 | **EDA notebook — a sample of every table reachable from the API** | 0 | Spec: `docs/notebook-specs.md` §1; heading hierarchy is the contract. Rendered via nb2report. **Queued 2026-09-14 after Marc asked where the notebooks were — a fair question: `notebooks/` had held one `.gitkeep` since round one while Cowork sequenced six rounds of plumbing. Cowork's sequencing error.** Rescoped to read the warehouse where data has landed and call the API live only for unextracted surfaces. §6 "What We Cannot Get" is now the document's most valuable page. Prompt: `prompts/spot-main-R-008.md`. |
| R-009 | 📋 | **Listening-patterns notebook — volume, music vs podcast, top podcasts, genre radar, longitudinal drill** | 0 | Spec: `docs/notebook-specs.md` §2. Depends on R-003 for podcast and duration data. |
| R-010 | ✅ | **Parallel VS Code sessions must work — worktrees with full connectivity and permissions** | 0 | "make sure the work pattern supports spinning subsequent VS Code sessions (worktrees)." Protocol in `docs/worktree-protocol.md`; enforcement lands with R-002's `make bootstrap`. |
| R-011 | ✅ | **NEVER let Spotify API connectivity data leak to public GitHub** | 0 | Marc, verbatim: "NEVER let any Spotify API connectivity data leak to GitHub." **Externally verified 2026-09-13 after the public push:** Cowork fetched `github.com/marc4data/spotify-lakehouse` as an anonymous reader — repo is Public, tree contains no `.env`, no `tokens*.json`, no `data/`, no executed notebooks. Full-history pattern scan clean across all 3 commits. Hooks installed and two of them proven to reject a staged 32-hex string. **Evidence basis corrected 2026-09-14 by R-020 §3:** a gitleaks-clean result was weaker evidence than Cowork treated it as — default rules missed a fake 32-hex `SPOTIFY_CLIENT_SECRET` outright, because `generic-api-key` carries a 3.5 Shannon-entropy floor and random hex tops out at 4.0, so a real share of Spotify values fall under it. The scan that actually covered this credential shape was the literal `[0-9a-f]{32}` pattern check. Now closed properly by the project `.gitleaks.toml` shipped in `4b4c312`. |
| R-012 | ✅ | **Hard separation — Cowork manages, Claude Code builds** | 0 | "Hard separation between Church and State." `CLAUDE.md` §1. Marc chose the "contracts specified, internals free" model. |
| R-013 | ✅ | **Guard against accidentally using Chat instead of Cowork for this project** | 0 | Marc: "Chat doesn't have enough memory or shared context and I consider using it a mistake." Two mechanisms: `CLAUDE.md` §3 (the register-table tell), and a project-memory note so a Chat session reads the rule and redirects him. |
| R-014 | ⏸️ | **Request Extended Streaming History for all four accounts** | 0 | Marc's action, not a build. Long lead time — this gates R-003 and half of R-009. |
| R-015 | 💭 | **Genre bucket taxonomy — sign-off needed** | 0 | **Deferred 2026-09-14, no longer Marc's to answer yet.** The 13 buckets presumed Spotify's micro-genre vocabulary, which this app does not receive. R-024 must choose a source first; MusicBrainz and Last.fm tags are different vocabularies. Not dropped — re-based. |
| R-016 | 📋 | **Verify empirically whether added dev-mode users need their own Premium** | 0 | Spotify's docs specify the app owner only. Unverified. Tested during family onboarding; code must handle the failure path either way. |
| R-017 | ✅ | **launchd agent — poll recently-played every 30 min for all profiles** | 0 | Marc chose 30 min. Installed once from `main`, shares the refresh advisory lock. **Unblocked 2026-09-14 by R-018: the poller is a catcher, not a backfiller.** No backward paging, so every play not captured before the 50-item window overflows is lost for good — until the export arrives. Rate observed in R-018: the window moved **14 plays in ~19 hours** for `marc`, so 30 minutes is generously safe on a light week and untested on a heavy one. **BUILT at `3b7bb5d`**, 16 code/config files verified present against the repo: `poller.py`, `migrations/0004`, the plist template with `StartInterval 1800`, `scripts/launchd.sh`, `scripts/refresh_agent.sh`, three new test modules; `detect_gap`'s real condition restored after the break. Round reports the agent loaded, fired at load, logged a real poll and exited 0 (run `refresh-main-8007683f`, poll_id 11, 20:43:02.577Z). **❓ not ✅ because Cowork cannot reach the log, the database or launchd** — and because the round itself draws the right line: installed is not running. R-029 converts it. **CONVERTED 2026-09-14 21:51 UTC — Marc ran `make refresh-status` and saw it: `runs 3`, last exit 0, last successful poll 21:43:08.427Z.** Load fired 20:43, then 21:13 and 21:43 — two firings at exactly 1800 s apart, so the interval works, not just `RunAtLoad`. And it is doing the job, not merely running: the high-water mark moved 19:25:08 → 21:40:35, so it captured plays Marc made this afternoon. 0 overflow gaps, 0 errors. |
| R-018 | ✅ | **Does `recently-played` page backwards via `next`?** | 0 | F10 from the R-002 report. Cowork asserted "cannot page backwards" more strongly than the evidence supported; the probe returned a non-null `next` with a `before=` cursor. One call settles it. `CLAUDE.md` §4 marked provisional. **ANSWERED: NO.** `fb24804`, verified against the repo — `spotify_lakehouse/paging.py` and `tests/test_paging.py` present, host allowlist at `paging.py:27`, `--follow-next` wired in `cli.py`. Following page 1's `next` returned 0 items and `next = null` while 14 older plays were demonstrably available from the same endpoint ~19 h earlier — the control that makes the empty page evidence. Recorded in `CLAUDE.md` §4 as a scoped measurement, not a flat claim. Unblocks R-017. |
| R-023 | ✅ | **Claude Code cannot see the account `project-round-close` skill** | 4 | Measured: the skill is enabled on the account and resolves in Cowork as bare `/project-round-close`; in Claude Code both the bare and `anthropic-skills:`-namespaced forms return *Unknown command*, and a window restart did not change it. `/cfdb-round` **does** resolve in that same window, so this is not "Claude Code cannot see account skills" — it is this skill specifically. Cause unknown and not guessable from Cowork. **Fix attempted:** a repo-local `.claude/commands/project-round-close.md` carrying only the execute half, so Cowork keeps the close format and there is no duplicated content to drift. Four rounds lost to this; Cowork spent three of them editing documentation instead of trying the one mechanism inside its own control. **LANDED 2026-09-14**, verified against the repo: `.claude/commands/project-round-close.md` is tracked at `2602221`, pushed, origin in sync, and Marc confirms the command is visible in the Claude Code picker after a restart. **Root cause, from the notso-prompt session:** account and plugin skills from claude.ai live only in the Cowork cloud container and are never installed on the Mac — so both the bare and namespaced forms were correctly unknown, and every Code session that said so was right while being asked again. **Still open:** the exact command string as the picker renders it, to be recorded in `CLAUDE.md` §2 only once observed. |
| R-020 | 🔨 | **CI must scan git history, not a commit range** | 0 | The first push's `gitleaks` job failed having scanned ~0 bytes. The reported cause (no-parent commit range on an initial commit) is an inference, not a confirmed mechanism — and it doesn't matter, because the real finding is underneath it: **neither CI job has ever scanned history.** `gitleaks-action` scans a pushed range; `checks` scans the current tree. A secret committed then removed passes both. **Built and pushed** — `4b4c312` (pinned binary, full-history scan, project `.gitleaks.toml`) and `e4e9109` (log scope + report). **Acceptance is partial:** the on-GitHub red run was correctly stopped per the prompt's stop condition; the guard was proven failing off-GitHub with the byte-identical script. **Cowork's call: route B** — a throwaway private repo gives a real red Actions run without putting a credential-shaped string in public history. Report surfaced a bigger finding than the one it was sent for (see R-011). Prompt: `prompts/spot-main-R-020.md`. **Handoff attempt 1 (2026-09-14) did not run** — the Code session answered about cfdb round A125 from the ncaa_football project instead. Prompt file verified present, readable and correctly pathed; repo contains no cfdb reference. Failure is in the session, not the artifact. Re-handed in a fresh session. See R-021. |
| R-021 | ⚠️ | **A Code session answered a different project's question and ignored the handoff** | 1 | Pasting the R-020 handoff into a `main` Code session returned a refusal about cfdb round `A125`, a different project. The word "still" in that reply means the session already held a cfdb exchange. **Cause not established — Cowork cannot see inside a Code session and will not guess.** Two candidates: stale session context, or the account-level `cfdb-round` skill re-triggering on shared vocabulary (both projects use "round", `claude_work/`, a request register, prompt files, and a §-numbered CLAUDE.md). Diagnostic: re-hand in a fresh session. Recorded as ⚠️ because a handoff that silently does nothing is a tracking failure whatever caused it. |

---

## spot-wta

| ID | Status | Request | Rounds | Notes |
|---|---|---|---|---|
| R-019 | ✅ | **Bring `spotify-wta` current with `main`** | 1 | **Landed and verified by Cowork against the repo, 2026-09-14** — `feat/probe` moved 3a499cf → e4e9109; `git show e4e9109:spotify_lakehouse/redact.py` contains `account_id`; GitHub CI was green on that exact commit, which covers pytest better than a local run would. **No report was written and none is required** — the outcome was directly observable. `spot probe` is safe from wta again. |
| R-022 | ✅ | **Uncommitted Cowork edits are invisible or stale to Code sessions** | 0 | The wta session reported "the only prompts are R-002 and R-018" and read R-019 as 📋 at register line 32. Both are wrong against the current working tree: R-020's prompt has been on disk since 06:41 UTC and line 32 has read 💭 since the round before. Cause is structural, not a session bug: **5 modified and 2 untracked Cowork files have sat uncommitted since the initial push.** A worktree sees only committed state; `main` sees whatever it last read. Cowork has listed "commit these" as an action item for three rounds and treated it as housekeeping. **CLOSED 2026-09-14** by commit `a0b2c74`: all four prompts are now tracked, working tree clean, origin in sync, and Cowork's stale `.git/index.lock` is gone. Original note follows. It was a blocker, and it was Cowork's. **Round 3 of the same failure, 2026-09-14:** `git ls-files` confirms `spot-main-R-002.md` is the *only* committed prompt — R-018, R-020 and R-019 are all untracked, so no worktree can open any of them. Compounded by Cowork leaving a stale `.git/index.lock` in the main checkout, which blocks the very commit that would fix this. **Protocol changed rather than re-asked:** `CLAUDE.md` §2 now forbids handing off an uncommitted prompt file and routes small rounds inline. |
| R-024 | 📋 | **Choose and build an external genre source** | 0 | Spotify supplies none. Cowork's call: **MusicBrainz**, joined ISRC → recording → artist (ISRC present on 100/100 items), because it needs no API key and therefore no new credential in a public repo (§5). `dim_artist` returns to SCD2 on the sourced genres plus `genre_source`. Unblocks R-015's re-based taxonomy and R-009's radar. |
| R-025 | 💭 | **Package the round workflow as a plugin instead of a skill** | 0 | Marc asked. **Answered NO by the notso-prompt project, 2026-09-14, on three measured grounds:** there is no documented path from a git-backed marketplace into Cowork (Claude Code's marketplace mechanics are documented; cross-surface install is not); plugin skills are **always** namespaced `/plugin:skill`, so a plugin would turn `/project-round-close spot-main-R-004` back into the namespaced string that cost four rounds; and per-project auto-install needs `.claude/settings.json` committed, which this repo gitignores for leak reasons. **Architecture instead:** one source (`~/notso_prompt/skill/SKILL.md`) → two generated artifacts, each carrying a provenance line — the account skill (both halves, Cowork) and this repo's `.claude/commands/project-round-close.md` (execute half only). **Revisit only when** four or more repos need the execute half, OR Anthropic documents a custom-marketplace path into Cowork. Not before. |
| R-026 | ✅ | **Skill edits route through notso-prompt, not this project** | 0 | Marc's rule, relayed 2026-09-14: notso-prompt owns `~/notso_prompt/skill/SKILL.md`; other surfaces propose changes there rather than editing the account skill directly, because unsynchronised edits silently overwrite each other — a save replaces the whole file, so the loser never learns it lost. Recorded in `CLAUDE.md` §2. **This project had already created the exact divergence the rule prevents:** two proposals were saved to the account skill at ~12:14 PM, after notso-prompt refreshed its source to the 11:03 AM version. Verified present in the account skill, absent from the source. The delta is itemised in the relay message and is notso-prompt's to merge. |
| R-027 | ✅ | **The pytest DB fixture applies migrations to the shared database** | 0 | C2 from R-004: `tests/test_migrate.py::conn` calls `migrate.apply`, and that is how `0002` and `0003` first landed rather than `make migrate` — a ruff failure stopped the chain before `spot migrate` and pytest ran after it. Idempotent and the in-transaction guard held, so no data finding. But a test run must not be what changes shared schema. Fix in the next round that touches tests: the fixture asserts migrations are current and **skips** otherwise, leaving `make migrate` the only writer. **CLOSED in R-017 §6 and proven the right way round:** with `0004` unapplied, all 9 DB tests skipped naming the pending migration, and `0004` was still absent afterwards — the fixture was shown not to write, rather than assumed not to. `tests/conftest.py:52-58` verified. |
| R-028 | ✅ | **Regenerate the repo-local command from the merged skill source** | 0 | notso-prompt merged all six spotify items into `~/notso_prompt/skill/SKILL.md` at 12:25 PM and Marc saved at 12:39:41 — before the relay arrived, so the divergence R-026 reported was real at 12:22 and already closed by 12:39. **Parity verified independently here:** sha256 of the account skill is `abb62e40…dee11`, 17,297 bytes, matching notso-prompt's claim exactly. Command file regenerated from it with the provenance header; currency is now a one-command hash check documented in `CLAUDE.md` §2. Three rules inherited from notso-prompt: numbered Action Items restarting at 1, abbreviations must say what they stand for, and "one tool's refusal is not a capability claim". |
| R-029 | ✅ | **Confirm the poller fires on its interval, not just at load** | 0 | R-017 F4, and the round's own distinction: only the `RunAtLoad` firing has been observed. Nothing yet proves launchd re-fires every 30 minutes, or what happens across Mac sleep, Docker stopped, or no network. `--status` flags a profile STALE after 60 minutes without a successful poll, which is the detector. **Check on 2026-09-15:** `make refresh-status` in the `main` terminal should show roughly 48 polls in 24 h and 0 errors. Fewer is the finding. **ANSWERED the same afternoon rather than the next day — Cowork had conflated two questions.** *Does the interval fire* was answerable 30 minutes after install; *does it survive a night* is the 24 h question. Measured at 21:51 UTC, 68 minutes after load: `runs 3` = load + 21:13 + 21:43, spacing exactly 1800 s. **Still unobserved:** behaviour across Mac sleep, Docker Desktop stopped, or no network. Not minting a round for it — the detector already exists: `--status` flags a profile STALE after 60 minutes without a successful poll, and a real overflow would write a gap row. If it bites, it announces itself. |
| R-030 | 📋 | **Ruff hooks reformat Cowork's prose files and bounce the commit** | 0 | Measured 2026-09-14: `git commit` of two markdown files failed on `ruff-format` — "1 file reformatted". The change was **one blank line inserted inside the ```python fence** in `prompts/spot-main-R-008.md` (index 107 lines / 6,037 b → worktree 108 / 6,038). Benign, but it aborts the commit and forces a second attempt, and `ruff-check` skipped the same files while `ruff-format` did not. **Fix:** exclude `claude_work/**` from both ruff hooks in `.pre-commit-config.yaml` — prompts and the register are prose, not source. Code's file, so it lands in the next round that touches config. |
| R-031 | 📋 | **Every handoff now needs a manual commit first, and a hook can bounce it** | 1 | Structural consequence of the boundary Marc set 2026-09-14: Cowork writes prompts but cannot commit, so **every** round starts with Code refusing an untracked prompt and Marc running git by hand — twice, when a hook modifies a file. Three interruptions to hand over one round. **Two fixes, both proposed not applied.** (a) The resolver's "written but untracked" state should have Code **commit it and proceed** rather than refuse — that state was written when Cowork could ask Marc to commit, which is no longer true. notso-prompt owns the skill (R-026), so this is a proposal. (b) For new projects, the PM folder lives **outside** the code repo, which removes the state entirely — no worktree, no committed-vs-working-tree gap. |
